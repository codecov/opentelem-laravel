version: 3

# FOSSA CLI for getsentry/action-enforce-license-compliance.
# https://github.com/fossas/fossa-cli/blob/master/docs/references/files/fossa-yml.md

project:
  id: github.com/codecov/opentelem-laravel
  url: https://github.com/codecov/opentelem-laravel

# FOSSA v3 discovers Composer projects via composer.lock (see fossa-cli composer strategy docs).
targets:
  only:
    - type: composer
      path: "./"

telemetry:
  scope: "off"
